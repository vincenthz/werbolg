#[cfg(test)]
mod tests {
    use werbolg_ir_write::module;

    #[test]
    fn it_compiles() {
        /*
        let mod1 = module! {
            fn add(a, b) {
                a + b
            }

            fn sub(a, b) {
                a - b
            }
        };
        assert!(false)
        */
    }
}
